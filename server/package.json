{
  "name": "casino-game-server",
  "version": "1.0.0",
  "description": "Server for the casino jackpot game using TypeScript and Bun",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.1.0",
    "bun-types": "latest",
    "typescript": "^5.5.4"
  }
}
