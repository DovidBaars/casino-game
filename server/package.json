{
  "name": "casino-jackpot-game-server",
  "version": "1.0.0",
  "description": "Server for the casino jackpot game using TypeScript and Bun",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^18.15.11",
    "bun-types": "latest",
    "typescript": "^4.9.5"
  }
}
